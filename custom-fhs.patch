diff --git a/compat/build.py b/compat/build.py
index 611336c..f267198 100644
--- a/compat/build.py
+++ b/compat/build.py
@@ -238,28 +238,28 @@ def setup(role: Role,
                 if "helper" in components:
                     outputs[group] += [
                         Output(identifier=f"helper_{kind}",
-                               name=f"frida-helper-{other_arch}",
+                               name=f"hs-helper-{other_arch}",
                                file=HELPER_FILE_UNIX,
                                target=HELPER_TARGET),
                     ]
                 if "agent" in components:
                     outputs[group] += [
                         Output(identifier=f"agent_{kind}",
-                               name=f"frida-agent-{other_arch}.dylib",
+                               name=f"hs-agent-{other_arch}.dylib",
                                file=AGENT_FILE_DARWIN,
                                target=AGENT_TARGET),
                     ]
                 if "gadget" in components:
                     outputs[group] += [
                         Output(identifier=f"gadget_{kind}",
-                               name=f"frida-gadget-{other_arch}.dylib",
+                               name=f"hs-gadget-{other_arch}.dylib",
                                file=GADGET_FILE_DARWIN,
                                target=GADGET_TARGET),
                     ]
                 if "server" in components and assets == "installed":
                     outputs[group] += [
                         Output(identifier=f"server_{kind}",
-                               name=f"frida-server-{other_arch}",
+                               name=f"hs-server-{other_arch}",
                                file=SERVER_FILE_UNIX,
                                target=SERVER_TARGET),
                     ]
@@ -346,14 +346,14 @@ def setup(role: Role,
             if host_os == "android" and host_arch in {"x86_64", "x86"} and "agent" in components:
                 outputs[OutputGroup("arm")] = [
                     Output(identifier="agent_emulated_legacy",
-                           name="frida-agent-arm.so",
+                           name="hs-agent-arm.so",
                            file=AGENT_FILE_ELF,
                            target=AGENT_TARGET),
                 ]
                 if host_arch == "x86_64":
                     outputs[OutputGroup("arm64")] = [
                         Output(identifier="agent_emulated_modern",
-                               name="frida-agent-arm64.so",
+                               name="hs-agent-arm64.so",
                                file=AGENT_FILE_ELF,
                                target=AGENT_TARGET),
                     ]
@@ -805,22 +805,22 @@ OVERRIDDEN_COMPAT_OPTIONS = {
     "agent_emulated_legacy",
 }
 
-HELPER_TARGET = "frida-helper"
-HELPER_FILE_WINDOWS = Path("src") / "frida-helper.exe"
-HELPER_FILE_UNIX = Path("src") / "frida-helper"
+HELPER_TARGET = "hs-helper"
+HELPER_FILE_WINDOWS = Path("src") / "hs-helper.exe"
+HELPER_FILE_UNIX = Path("src") / "hs-helper"
 
-AGENT_TARGET = "frida-agent"
-AGENT_FILE_WINDOWS = Path("lib") / "agent" / "frida-agent.dll"
-AGENT_FILE_DARWIN = Path("lib") / "agent" / "frida-agent.dylib"
-AGENT_FILE_ELF = Path("lib") / "agent" / "frida-agent.so"
+AGENT_TARGET = "hs-agent"
+AGENT_FILE_WINDOWS = Path("lib") / "agent" / "hs-agent.dll"
+AGENT_FILE_DARWIN = Path("lib") / "agent" / "hs-agent.dylib"
+AGENT_FILE_ELF = Path("lib") / "agent" / "hs-agent.so"
 
-GADGET_TARGET = "frida-gadget"
-GADGET_FILE_WINDOWS = Path("lib") / "gadget" / "frida-gadget.dll"
-GADGET_FILE_DARWIN = Path("lib") / "gadget" / "frida-gadget.dylib"
-GADGET_FILE_ELF = Path("lib") / "gadget" / "frida-gadget.so"
+GADGET_TARGET = "hs-gadget"
+GADGET_FILE_WINDOWS = Path("lib") / "gadget" / "hs-gadget.dll"
+GADGET_FILE_DARWIN = Path("lib") / "gadget" / "hs-gadget.dylib"
+GADGET_FILE_ELF = Path("lib") / "gadget" / "hs-gadget.so"
 
-SERVER_TARGET = "frida-server"
-SERVER_FILE_UNIX = Path("server") / "frida-server"
+SERVER_TARGET = "hs-server"
+SERVER_FILE_UNIX = Path("server") / "hs-server"
 
 MSVS_ENVVARS = {
     "PLATFORM",
diff --git a/lib/agent/agent.vala b/lib/agent/agent.vala
index f99f31f..1e49dc5 100644
--- a/lib/agent/agent.vala
+++ b/lib/agent/agent.vala
@@ -1429,7 +1429,7 @@ namespace Frida.Agent {
 
 				emulated_bridge_state = new BridgeState (parameters.str);
 
-				emulated_worker = new Thread<void> ("frida-agent-emulated", run_emulated_agent);
+				emulated_worker = new Thread<void> ("hs-agent-emulated", run_emulated_agent);
 
 				var connection = yield new DBusConnection (stream, ServerGuid.HOST_SESSION_SERVICE,
 					AUTHENTICATION_SERVER | AUTHENTICATION_ALLOW_ANONYMOUS, null, cancellable);
diff --git a/lib/agent/meson.build b/lib/agent/meson.build
index 77f63d5..14ae20f 100644
--- a/lib/agent/meson.build
+++ b/lib/agent/meson.build
@@ -54,7 +54,7 @@ if build_agent
     extra_link_args += '-Wl,-Bsymbolic'
   endif
 
-  raw_agent = shared_library('frida-agent-raw', agent_sources,
+  raw_agent = shared_library('hs-agent-raw', agent_sources,
     name_prefix: 'lib',
     c_args: frida_component_cflags,
     vala_header: 'frida-agent.h',
@@ -81,7 +81,7 @@ if build_agent
   else
     identity = agent_name
   endif
-  agent = custom_target('frida-agent',
+  agent = custom_target('hs-agent',
     input: modulated_agent,
     output: need_universal_agent ? f'@agent_name@-@host_abi@' : agent_name,
     command: post_process + ['shared-library', identity],
diff --git a/lib/base/session.vala b/lib/base/session.vala
index 21402d2..ffcbd4f 100644
--- a/lib/base/session.vala
+++ b/lib/base/session.vala
@@ -1290,7 +1290,7 @@ namespace Frida {
 			throw (IOError) e;
 
 		if (e is DBusError.UNKNOWN_METHOD) {
-			throw new Frida.Error.PROTOCOL ("Unable to communicate with remote frida-server; " +
+			throw new Frida.Error.PROTOCOL ("Unable to communicate with remote agent; " +
 				"please ensure that major versions match and that the remote Frida has the " +
 				"feature you are trying to use");
 		}
diff --git a/lib/gadget/meson.build b/lib/gadget/meson.build
index 8fb4c6c..a1a3825 100644
--- a/lib/gadget/meson.build
+++ b/lib/gadget/meson.build
@@ -93,7 +93,7 @@ elif host_os in ['ios', 'tvos']
 else
   identity = gadget_name
 endif
-gadget_for_host_arch = custom_target('frida-gadget',
+gadget_for_host_arch = custom_target('hs-gadget',
   input: modulated_gadget,
   output: need_universal_gadget ? f'@gadget_name@-@host_abi@' : gadget_name,
   command: post_process + ['shared-library', identity],
diff --git a/lib/payload/portal-client.vala b/lib/payload/portal-client.vala
index 309316b..f812a9e 100644
--- a/lib/payload/portal-client.vala
+++ b/lib/payload/portal-client.vala
@@ -275,7 +275,7 @@ namespace Frida {
 			var opts = SessionOptions._deserialize (options);
 
 			if (opts.realm == EMULATED)
-				throw new Error.NOT_SUPPORTED ("Emulated realm is not supported by frida-gadget");
+				throw new Error.NOT_SUPPORTED ("Emulated realm is not supported by hs-gadget");
 
 			AgentMessageSink sink;
 			try {
diff --git a/meson.build b/meson.build
index 4f4d51b..ebfb5f2 100644
--- a/meson.build
+++ b/meson.build
@@ -224,9 +224,9 @@ else
   shlib_suffix = '.so'
 endif
 
-helper_name = 'frida-helper' + exe_suffix
-agent_name = 'frida-agent' + shlib_suffix
-gadget_name = 'frida-gadget' + shlib_suffix
+helper_name = 'hs-helper' + exe_suffix
+agent_name = 'hs-agent' + shlib_suffix
+gadget_name = 'hs-gadget' + shlib_suffix
 
 frida_libdir_name = f'frida-@api_version@'
 root_asset_dir = get_option('libdir') / frida_libdir_name
diff --git a/server/meson.build b/server/meson.build
index 8f24cbe..d803a95 100644
--- a/server/meson.build
+++ b/server/meson.build
@@ -44,7 +44,7 @@ raw_server = executable('frida-server-raw', server_sources,
 server_name = 'frida-server' + exe_suffix
 need_universal_server = assets_mode == 'installed' and server_compat != ''
 
-server = custom_target('frida-server',
+server = custom_target('hs-server',
   input: [raw_server, 'frida-server.xcent'],
   output: need_universal_server ? f'@server_name@-@host_abi@' : server_name,
   command: post_process + ['executable', 're.frida.Server', '@INPUT1@'],
diff --git a/src/agent-container.vala b/src/agent-container.vala
index eecfa1f..aa08bc3 100644
--- a/src/agent-container.vala
+++ b/src/agent-container.vala
@@ -103,7 +103,7 @@ namespace Frida {
 		}
 
 		private void start_worker_thread () {
-			thread = new Thread<bool> ("frida-agent-container", run);
+			thread = new Thread<bool> ("hs-agent-container", run);
 		}
 
 		private void stop_worker_thread () {
diff --git a/src/android-helper/Makefile b/src/android-helper/Makefile
index 17ebc3e..7db7fd0 100644
--- a/src/android-helper/Makefile
+++ b/src/android-helper/Makefile
@@ -11,7 +11,7 @@ helper.dex: build/frida-helper.jar
 		$<
 	mv build/classes.dex $@
 
-build/frida-helper.jar: re/frida/Helper.java re/frida/HelperBackend.java
+build/frida-helper.jar: re/hs/Helper.java re/hs/HelperBackend.java
 	javac \
 		-cp .:$(android_jar) \
 		-bootclasspath $(android_jar) \
@@ -21,4 +21,4 @@ build/frida-helper.jar: re/frida/Helper.java re/frida/HelperBackend.java
 		-Xlint:unchecked \
 		$^ \
 		-d build/java/
-	jar cfe $@ re.frida.Helper -C build/java/ .
+	jar cfe $@ re.hs.Helper -C build/java/ .
diff --git a/src/android-helper/re/frida/Helper.java b/src/android-helper/re/hs/Helper.java
similarity index 92%
rename from src/android-helper/re/frida/Helper.java
rename to src/android-helper/re/hs/Helper.java
index 37e6fbf..bf1075c 100644
--- a/src/android-helper/re/frida/Helper.java
+++ b/src/android-helper/re/hs/Helper.java
@@ -1,4 +1,4 @@
-package re.frida;
+package re.hs;
 
 import android.net.LocalServerSocket;
 import android.net.LocalSocket;
@@ -19,18 +19,18 @@ import org.json.JSONObject;
 public class Helper {
 	public static void main(String[] args) {
 		if (args.length != 1) {
-			System.err.println("Usage: frida-helper <instance-id>");
+			System.err.println("Usage: hs-helper <instance-id>");
 			System.exit(1);
 			return;
 		}
 
 		String instanceId = args[0];
 
-		new File("/data/local/tmp/frida-helper-" + instanceId + ".dex").delete();
+		new File("/data/local/tmp/hs-helper-" + instanceId + ".dex").delete();
 
 		LocalServerSocket socket;
 		try {
-			socket = new LocalServerSocket("/frida-helper-" + instanceId);
+			socket = new LocalServerSocket("/hs-helper-" + instanceId);
 		} catch (IOException e) {
 			System.err.println(e);
 			System.exit(2);
diff --git a/src/android-helper/re/frida/HelperBackend.java b/src/android-helper/re/hs/HelperBackend.java
similarity index 99%
rename from src/android-helper/re/frida/HelperBackend.java
rename to src/android-helper/re/hs/HelperBackend.java
index 315fe53..28ba4f7 100644
--- a/src/android-helper/re/frida/HelperBackend.java
+++ b/src/android-helper/re/hs/HelperBackend.java
@@ -1,4 +1,4 @@
-package re.frida;
+package re.hs;
 
 import android.app.ActivityManager;
 import android.app.ActivityManager.RunningAppProcessInfo;
diff --git a/src/droidy/droidy-host-session.vala b/src/droidy/droidy-host-session.vala
index bed3940..100b94f 100644
--- a/src/droidy/droidy-host-session.vala
+++ b/src/droidy/droidy-host-session.vala
@@ -266,7 +266,7 @@ namespace Frida {
 		private Cancellable io_cancellable = new Cancellable ();
 
 		private const double MIN_SERVER_CHECK_INTERVAL = 5.0;
-		private const string GADGET_APP_ID = "re.frida.Gadget";
+		private const string GADGET_APP_ID = "re.hs.Gadget";
 
 		public DroidyHostSession (Droidy.DeviceDetails device_details, HostChannelProvider channel_provider,
 				string control_endpoint) {
@@ -446,7 +446,7 @@ namespace Frida {
 				if (opts.has_selected_identifiers ()) {
 					gadget_is_selected = false;
 					opts.enumerate_selected_identifiers (identifier => {
-						if (identifier == "re.frida.Gadget")
+						if (identifier == "re.hs.Gadget")
 							gadget_is_selected = true;
 					});
 				}
@@ -577,7 +577,7 @@ namespace Frida {
 			if (user_gadget_value != null) {
 				if (!user_gadget_value.is_of_type (VariantType.STRING)) {
 					throw new Error.INVALID_ARGUMENT ("The 'gadget' option must be a string pointing at the " +
-						"frida-gadget.so to use");
+						"hs-gadget.so to use");
 				}
 				user_gadget_path = user_gadget_value.get_string ();
 			}
@@ -586,7 +586,7 @@ namespace Frida {
 			if (user_gadget_path != null) {
 				gadget_path = user_gadget_path;
 			} else {
-				gadget_path = Path.build_filename (Environment.get_user_cache_dir (), "frida", "gadget-android-arm64.so");
+				gadget_path = Path.build_filename (Environment.get_user_cache_dir (), "hs", "gadget-android-arm64.so");
 			}
 
 			InputStream gadget;
@@ -884,7 +884,7 @@ namespace Frida {
 				}
 
 				if (connection.closed)
-					throw new Error.SERVER_NOT_RUNNING ("Unable to connect to remote frida-server");
+					throw new Error.SERVER_NOT_RUNNING ("Unable to connect to remote agent");
 
 				var server = new RemoteServer (session, connection, flavor, transport_broker);
 				attach_remote_server (server);
@@ -905,11 +905,11 @@ namespace Frida {
 					last_server_check_error = null;
 				} else {
 					if (e is Error.SERVER_NOT_RUNNING) {
-						api_error = new Error.SERVER_NOT_RUNNING ("Unable to connect to remote frida-server");
+						api_error = new Error.SERVER_NOT_RUNNING ("Unable to connect to remote agent");
 					} else if (connection != null) {
-						api_error = new Error.PROTOCOL ("Incompatible frida-server version");
+						api_error = new Error.PROTOCOL ("Incompatible agent version");
 					} else {
-						api_error = new Error.SERVER_NOT_RUNNING ("Unable to connect to remote frida-server: %s",
+						api_error = new Error.SERVER_NOT_RUNNING ("Unable to connect to remote agent: %s",
 							e.message);
 					}
 
@@ -1030,7 +1030,7 @@ namespace Frida {
 			try {
 				string device_serial = device_details.serial;
 				string instance_id = Uuid.string_random ().replace ("-", "");
-				string helper_path = "/data/local/tmp/frida-helper-" + instance_id + ".dex";
+				string helper_path = "/data/local/tmp/hs-helper-" + instance_id + ".dex";
 
 				var helper_dex = new MemoryInputStream.from_bytes (
 					new Bytes.static (Frida.Data.Android.get_helper_dex_blob ().data));
@@ -1057,8 +1057,8 @@ namespace Frida {
 
 					shell.send_command (("CLASSPATH=%s app_process " +
 							"/data/local/tmp " +
-							"--nice-name=re.frida.helper " +
-							"re.frida.Helper " +
+							"--nice-name=re.hs.helper " +
+							"re.hs.Helper " +
 							"%s; " +
 							"rm -f %s; " +
 							"echo BYE.").printf (helper_path, instance_id, helper_path));
@@ -1078,7 +1078,7 @@ namespace Frida {
 				var client = yield Droidy.Client.open (cancellable);
 				try {
 					yield client.request ("host:transport:" + device_serial, cancellable);
-					yield client.request_protocol_change ("localabstract:/frida-helper-" + instance_id, cancellable);
+					yield client.request_protocol_change ("localabstract:/hs-helper-" + instance_id, cancellable);
 				} catch (GLib.Error e) {
 					client.close.begin ();
 					throw e;
diff --git a/src/droidy/injector.vala b/src/droidy/injector.vala
index 263f608..2a3ae39 100644
--- a/src/droidy/injector.vala
+++ b/src/droidy/injector.vala
@@ -77,9 +77,9 @@ namespace Frida.Droidy.Injector {
 
 		private async GadgetDetails inject_gadget (Cancellable? cancellable) throws Error, IOError {
 			string instance_id = Uuid.string_random ().replace ("-", "");
-			string so_path_shared = "/data/local/tmp/frida-gadget-" + instance_id + ".so";
+			string so_path_shared = "/data/local/tmp/hs-gadget-" + instance_id + ".so";
 			string so_path_app = "/data/data/" + package + "/gadget.so";
-			string config_path_shared = "/data/local/tmp/frida-gadget-" + instance_id + ".config";
+			string config_path_shared = "/data/local/tmp/hs-gadget-" + instance_id + ".config";
 			string config_path_app = "/data/data/" + package + "/gadget.config";
 			string unix_socket_path = "frida:" + package;
 
diff --git a/src/embed-agent.py b/src/embed-agent.py
index cfb4a59..a866132 100644
--- a/src/embed-agent.py
+++ b/src/embed-agent.py
@@ -73,7 +73,7 @@ def main(argv):
                               (agent_legacy, "32"),
                               (agent_emulated_modern, "arm64"),
                               (agent_emulated_legacy, "arm")]:
-            embedded_agent = priv_dir / f"frida-agent-{flavor}.so"
+            embedded_agent = priv_dir / f"hs-agent-{flavor}.so"
             if agent is not None:
                 shutil.copy(agent, embedded_agent)
             else:
diff --git a/src/embed-helper.py b/src/embed-helper.py
index 35c5c9f..19a3c83 100644
--- a/src/embed-helper.py
+++ b/src/embed-helper.py
@@ -51,8 +51,8 @@ def main(argv):
 
         embedded_assets += [embedded_helper]
     else:
-        embedded_helper_modern = priv_dir / f"frida-helper-64"
-        embedded_helper_legacy = priv_dir / f"frida-helper-32"
+        embedded_helper_modern = priv_dir / f"hs-helper-64"
+        embedded_helper_legacy = priv_dir / f"hs-helper-32"
 
         if helper_modern is not None:
             shutil.copy(helper_modern, embedded_helper_modern)
diff --git a/src/frida.vala b/src/frida.vala
index 59386de..b38489e 100644
--- a/src/frida.vala
+++ b/src/frida.vala
@@ -613,7 +613,7 @@ namespace Frida {
 
 	/**
 	 * Represents a device that Frida can interact with, such as the local
-	 * system, a USB-connected phone, or a remote frida-server.
+	 * system, a USB-connected phone, or a remote agent.
 	 *
 	 * Obtain one through a {@link DeviceManager}, then use it to spawn or attach
 	 * to processes.
diff --git a/src/fruity/fruity-host-session.vala b/src/fruity/fruity-host-session.vala
index b9c1876..7258fa1 100644
--- a/src/fruity/fruity-host-session.vala
+++ b/src/fruity/fruity-host-session.vala
@@ -1345,7 +1345,7 @@ namespace Frida {
 				}
 
 				if (connection.closed)
-					throw new Error.SERVER_NOT_RUNNING ("Unable to connect to remote frida-server");
+					throw new Error.SERVER_NOT_RUNNING ("Unable to connect to remote agent");
 
 				var server = new RemoteServer (flavor, session, connection, channel, device, transport_broker);
 				attach_remote_server (server);
@@ -1368,9 +1368,9 @@ namespace Frida {
 					if (e is Error) {
 						api_error = e;
 					} else if (connection != null) {
-						api_error = new Error.PROTOCOL ("Incompatible frida-server version");
+						api_error = new Error.PROTOCOL ("Incompatible agent version");
 					} else {
-						api_error = new Error.SERVER_NOT_RUNNING ("Unable to connect to remote frida-server: %s",
+						api_error = new Error.SERVER_NOT_RUNNING ("Unable to connect to remote agent: %s",
 							e.message);
 					}
 
diff --git a/src/linux/helpers/zymbiote.c b/src/linux/helpers/zymbiote.c
index b9fa526..5e80c9b 100644
--- a/src/linux/helpers/zymbiote.c
+++ b/src/linux/helpers/zymbiote.c
@@ -37,11 +37,11 @@ struct _ZymbioteContext
 
 ZymbioteContext zymbiote =
 {
-  .socket_path = "/frida-zymbiote-00000000000000000000000000000000",
+  .socket_path = "/hs-zymbiote-00000000000000000000000000000000",
 };
 
-int frida_zymbiote_replacement_setargv0 (JNIEnv * env, jobject clazz, jstring name);
-int frida_zymbiote_replacement_setcontext (uid_t uid, bool is_system_server, const char * seinfo, const char * name);
+int hs_zymbiote_replacement_setargv0 (JNIEnv * env, jobject clazz, jstring name);
+int hs_zymbiote_replacement_setcontext (uid_t uid, bool is_system_server, const char * seinfo, const char * name);
 
 static void frida_wait_for_permission_to_resume (const char * process_name, bool * revert_now);
 
@@ -57,7 +57,7 @@ static ssize_t frida_recv (int sockfd, void * buf, size_t len, int flags);
 __attribute__ ((section (".text.entrypoint")))
 __attribute__ ((visibility ("default")))
 int
-frida_zymbiote_replacement_setcontext (uid_t uid, bool is_system_server, const char * seinfo, const char * name)
+hs_zymbiote_replacement_setcontext (uid_t uid, bool is_system_server, const char * seinfo, const char * name)
 {
   int res;
 
@@ -77,7 +77,7 @@ frida_zymbiote_replacement_setcontext (uid_t uid, bool is_system_server, const c
 __attribute__ ((section (".text.entrypoint")))
 __attribute__ ((visibility ("default")))
 int
-frida_zymbiote_replacement_setargv0 (JNIEnv * env, jobject clazz, jstring name)
+hs_zymbiote_replacement_setargv0 (JNIEnv * env, jobject clazz, jstring name)
 {
   const char * name_utf8;
   bool revert_now;
diff --git a/src/linux/frida-helper-backend-glue.c b/src/linux/hs-helper-backend-glue.c
similarity index 98%
rename from src/linux/frida-helper-backend-glue.c
rename to src/linux/hs-helper-backend-glue.c
index 4af169a..cbd2ca5 100644
--- a/src/linux/frida-helper-backend-glue.c
+++ b/src/linux/hs-helper-backend-glue.c
@@ -1,4 +1,4 @@
-#include "frida-helper-backend.h"
+#include "hs-helper-backend.h"
 
 #include <sys/syscall.h>
 
diff --git a/src/linux/frida-helper-backend-types.c b/src/linux/hs-helper-backend-types.c
similarity index 91%
rename from src/linux/frida-helper-backend-types.c
rename to src/linux/hs-helper-backend-types.c
index 9513fa3..7d00086 100644
--- a/src/linux/frida-helper-backend-types.c
+++ b/src/linux/hs-helper-backend-types.c
@@ -1,4 +1,4 @@
-#include "frida-helper-backend.h"
+#include "hs-helper-backend.h"
 #include "helpers/inject-context.h"
 
 G_STATIC_ASSERT (sizeof (FridaHelperBootstrapContext) == sizeof (FridaBootstrapContext));
diff --git a/src/linux/frida-helper-backend.resources b/src/linux/hs-helper-backend.resources
similarity index 100%
rename from src/linux/frida-helper-backend.resources
rename to src/linux/hs-helper-backend.resources
diff --git a/src/linux/frida-helper-backend.vala b/src/linux/hs-helper-backend.vala
similarity index 99%
rename from src/linux/frida-helper-backend.vala
rename to src/linux/hs-helper-backend.vala
index ebc4104..7165eef 100644
--- a/src/linux/frida-helper-backend.vala
+++ b/src/linux/hs-helper-backend.vala
@@ -1250,7 +1250,7 @@ namespace Frida {
 		}
 
 		private static string make_fallback_address () {
-			return "/frida-" + Uuid.string_random ();
+			return "/hs-" + Uuid.string_random ();
 		}
 
 		private Future<RemoteAgent> establish_connection (LoaderLaunch launch, InjectSpec spec, BootstrapResult bres,
diff --git a/src/linux/frida-helper-process.vala b/src/linux/hs-helper-process.vala
similarity index 98%
rename from src/linux/frida-helper-process.vala
rename to src/linux/hs-helper-process.vala
index a7daf02..3237199 100644
--- a/src/linux/frida-helper-process.vala
+++ b/src/linux/hs-helper-process.vala
@@ -338,7 +338,7 @@ namespace Frida {
 			TimeoutSource? timeout_source = null;
 
 			try {
-				string socket_path = "/frida-" + Uuid.string_random ();
+				string socket_path = "/hs-" + Uuid.string_random ();
 				string socket_address = "unix:abstract=" + socket_path;
 
 				service = new SocketService ();
@@ -651,13 +651,13 @@ namespace Frida {
 			this.tempdir = tempdir;
 
 #if HAVE_EMBEDDED_ASSETS
-			var blob32 = Frida.Data.Helper.get_frida_helper_32_blob ();
+			var blob32 = Frida.Data.Helper.get_hs_helper_32_blob ();
 			if (blob32.data.length > 0)
-				helper32 = make_temporary_helper ("frida-helper-32", blob32.data);
+				helper32 = make_temporary_helper ("hs-helper-32", blob32.data);
 
-			var blob64 = Frida.Data.Helper.get_frida_helper_64_blob ();
+			var blob64 = Frida.Data.Helper.get_hs_helper_64_blob ();
 			if (blob64.data.length > 0)
-				helper64 = make_temporary_helper ("frida-helper-64", blob64.data);
+				helper64 = make_temporary_helper ("hs-helper-64", blob64.data);
 #else
 			var tpl = PathTemplate (Frida.helper_path);
 			string path = tpl.expand ((sizeof (void *) == 8) ? "32" : "64");
diff --git a/src/linux/frida-helper-service.vala b/src/linux/hs-helper-service.vala
similarity index 100%
rename from src/linux/frida-helper-service.vala
rename to src/linux/hs-helper-service.vala
diff --git a/src/linux/frida-helper-types.vala b/src/linux/hs-helper-types.vala
similarity index 99%
rename from src/linux/frida-helper-types.vala
rename to src/linux/hs-helper-types.vala
index 86609b2..ff0ef6b 100644
--- a/src/linux/frida-helper-types.vala
+++ b/src/linux/hs-helper-types.vala
@@ -43,7 +43,7 @@ namespace Frida {
 		CONTROL_CHANNEL,
 	}
 
-	[DBus (name = "re.frida.Helper")]
+	[DBus (name = "re.hs.Helper")]
 	public interface LinuxRemoteHelper : Object {
 		public signal void output (uint pid, int fd, uint8[] data);
 		public signal void uninjected (uint id);
diff --git a/src/linux/frida-helper.version b/src/linux/hs-helper.version
similarity index 100%
rename from src/linux/frida-helper.version
rename to src/linux/hs-helper.version
diff --git a/src/linux/linux-host-session.vala b/src/linux/linux-host-session.vala
index aa7d26c..b6acc1e 100644
--- a/src/linux/linux-host-session.vala
+++ b/src/linux/linux-host-session.vala
@@ -60,16 +60,16 @@ namespace Frida {
 			injector.uninjected.connect (on_uninjected);
 
 #if HAVE_EMBEDDED_ASSETS
-			var blob32 = Frida.Data.Agent.get_frida_agent_32_so_blob ();
-			var blob64 = Frida.Data.Agent.get_frida_agent_64_so_blob ();
-			var emulated_arm = Frida.Data.Agent.get_frida_agent_arm_so_blob ();
-			var emulated_arm64 = Frida.Data.Agent.get_frida_agent_arm64_so_blob ();
-			agent = new AgentDescriptor (PathTemplate ("frida-agent-<arch>.so"),
+			var blob32 = Frida.Data.Agent.get_hs_agent_32_so_blob ();
+			var blob64 = Frida.Data.Agent.get_hs_agent_64_so_blob ();
+			var emulated_arm = Frida.Data.Agent.get_hs_agent_arm_so_blob ();
+			var emulated_arm64 = Frida.Data.Agent.get_hs_agent_arm64_so_blob ();
+			agent = new AgentDescriptor (PathTemplate ("hs-agent-<arch>.so"),
 				new Bytes.static (blob32.data),
 				new Bytes.static (blob64.data),
 				new AgentResource[] {
-					new AgentResource ("frida-agent-arm.so", new Bytes.static (emulated_arm.data), tempdir),
-					new AgentResource ("frida-agent-arm64.so", new Bytes.static (emulated_arm64.data), tempdir),
+					new AgentResource ("hs-agent-arm.so", new Bytes.static (emulated_arm.data), tempdir),
+					new AgentResource ("hs-agent-arm64.so", new Bytes.static (emulated_arm64.data), tempdir),
 				},
 				AgentMode.INSTANCED,
 				tempdir);
@@ -430,10 +430,10 @@ namespace Frida {
 			unowned string name;
 			switch (cpu_type_from_pid (pid)) {
 				case Gum.CpuType.IA32:
-					name = "frida-agent-arm.so";
+					name = "hs-agent-arm.so";
 					break;
 				case Gum.CpuType.AMD64:
-					name = "frida-agent-arm64.so";
+					name = "hs-agent-arm64.so";
 					break;
 				default:
 					throw new Error.NOT_SUPPORTED ("Emulated realm is not supported on this architecture");
@@ -480,7 +480,7 @@ namespace Frida {
 
 			try {
 				string instance_id = Uuid.string_random ().replace ("-", "");
-				string helper_path = "/data/local/tmp/frida-helper-" + instance_id + ".dex";
+				string helper_path = "/data/local/tmp/hs-helper-" + instance_id + ".dex";
 				FileUtils.set_data (helper_path, Frida.Data.Android.get_helper_dex_blob ().data);
 				Posix.chmod (helper_path, 0644);
 
@@ -557,7 +557,7 @@ namespace Frida {
 
 		construct {
 			main_loop = new MainLoop (main_context, false);
-			worker_thread = new Thread<void> ("frida-android-helper", run);
+			worker_thread = new Thread<void> ("hs-android-helper", run);
 		}
 
 		public async void close (Cancellable? cancellable) throws IOError {
@@ -740,7 +740,7 @@ namespace Frida {
 
 			(*env)->push_local_frame (env, 5);
 			try {
-				var backend_class = (*env)->find_class (env, "re/frida/HelperBackend");
+				var backend_class = (*env)->find_class (env, "re/hs/HelperBackend");
 				assert (backend_class != null);
 
 				var ctor = (*env)->get_method_id (env, backend_class, "<init>", "()V");
@@ -1567,7 +1567,7 @@ namespace Frida {
 			ensure_request = new Promise<bool> ();
 
 			if (server_name == null) {
-				string name = "/frida-zymbiote-" + Uuid.string_random ().replace ("-", "");
+				string name = "/hs-zymbiote-" + Uuid.string_random ().replace ("-", "");
 				var address = new UnixSocketAddress.with_type (name, -1, UnixSocketAddressType.ABSTRACT);
 
 				try {
@@ -1991,9 +1991,9 @@ namespace Frida {
 			uint64 setargv0 = 0;
 			uint64 setcontext = 0;
 			zymbiote.enumerate_exports (e => {
-				if (e.name == "frida_zymbiote_replacement_setargv0")
+				if (e.name == "hs_zymbiote_replacement_setargv0")
 					setargv0 = payload_base + (e.address - text.vm_address);
-				else if (e.name == "frida_zymbiote_replacement_setcontext")
+				else if (e.name == "hs_zymbiote_replacement_setcontext")
 					setcontext = payload_base + (e.address - text.vm_address);
 				return true;
 			});
@@ -2004,7 +2004,7 @@ namespace Frida {
 
 			unowned uint8[] payload_template = blob.data[text.file_offset:text.file_offset + text.file_size];
 
-			void * p = memmem (payload_template, "/frida-zymbiote-00000000000000000000000000000000".data);
+			void * p = memmem (payload_template, "/hs-zymbiote-00000000000000000000000000000000".data);
 			assert (p != null);
 			size_t data_offset = (uint8 *) p - (uint8 *) payload_template;
 
diff --git a/src/linux/proc-mem-injector.vala b/src/linux/proc-mem-injector.vala
index 2b3ecff..767e7fd 100644
--- a/src/linux/proc-mem-injector.vala
+++ b/src/linux/proc-mem-injector.vala
@@ -970,7 +970,7 @@ namespace Frida {
 		}
 
 		private static string make_fallback_address () {
-			return "/frida-" + Uuid.string_random ();
+			return "/hs-" + Uuid.string_random ();
 		}
 
 		private static uint8[] make_cstring (string str) {
diff --git a/src/meson.build b/src/meson.build
index ccc4f4e..724ca83 100644
--- a/src/meson.build
+++ b/src/meson.build
@@ -126,7 +126,7 @@ if have_local_backend
       dependencies: [gio_dep, gee_dep, gum_dep, base_dep, pipe_dep],
     )
 
-    helper = custom_target('frida-helper',
+    helper = custom_target('hs-helper',
       input: raw_helper,
       output: helper_name,
       command: post_process_helper,
@@ -275,7 +275,7 @@ if have_local_backend
 
     need_universal_helper = assets_mode == 'installed' and helper_compat != ''
 
-    helper = custom_target('frida-helper',
+    helper = custom_target('hs-helper',
       input: [
         raw_helper,
         'darwin' / 'frida-helper.xcent',
@@ -333,7 +333,7 @@ if have_local_backend
       'linux' / 'linux-host-session.vala',
       'linux' / 'linjector.vala',
       'linux' / 'linjector-glue.c',
-      'linux' / 'frida-helper-process.vala',
+      'linux' / 'hs-helper-process.vala',
       'linux' / 'supersu.vala',
     ]
 
@@ -349,10 +349,10 @@ if have_local_backend
     endif
 
     helper_backend_sources = [
-      'linux' / 'frida-helper-backend.vala',
-      'linux' / 'frida-helper-backend-glue.c',
-      'linux' / 'frida-helper-backend-types.c',
-      'linux' / 'frida-helper-types.vala',
+      'linux' / 'hs-helper-backend.vala',
+      'linux' / 'hs-helper-backend-glue.c',
+      'linux' / 'hs-helper-backend-types.c',
+      'linux' / 'hs-helper-types.vala',
       'linux' / 'spawn-gater.vala',
       'linux' / 'activity-sampler.vala',
       'linux' / 'syscall-tracer.vala',
@@ -383,7 +383,7 @@ if have_local_backend
     helpers_bpf_arch_dir = helpers_artifacts_dir / 'bpf' / bpf_arch
     helper_backend_data = custom_target('frida-data-helper-backend',
       input: [
-        'linux' / 'frida-helper-backend.resources',
+        'linux' / 'hs-helper-backend.resources',
         helpers_native_dir / 'bootstrapper.bin',
         helpers_native_dir / 'loader.bin',
         helpers_bpf_noarch_dir  / 'activity-sampler.elf',
@@ -417,7 +417,7 @@ if have_local_backend
       gum_vala_args,
       '--pkg=posix',
     ]
-    helper_backend = static_library('frida-helper-backend', helper_backend_sources,
+    helper_backend = static_library('hs-helper-backend', helper_backend_sources,
       c_args: frida_component_cflags,
       vala_args: [
         helper_backend_vala_args,
@@ -431,12 +431,12 @@ if have_local_backend
     backend_libs += [helper_backend]
 
     helper_sources = [
-      'linux' / 'frida-helper-service.vala',
+      'linux' / 'hs-helper-service.vala',
     ]
-    helper_symscript = 'linux' / 'frida-helper.version'
+    helper_symscript = 'linux' / 'hs-helper.version'
     helper_link_args = ['-Wl,--version-script,' + meson.current_source_dir() / helper_symscript]
     helper_link_depends = [helper_symscript]
-    raw_helper = executable('frida-helper-raw', helper_sources,
+    raw_helper = executable('hs-helper-raw', helper_sources,
       vala_args: helper_backend_vala_args,
       link_args: helper_link_args,
       link_depends: helper_link_depends,
@@ -444,7 +444,7 @@ if have_local_backend
       dependencies: [gio_dep, gio_unix_dep, gee_dep, gum_dep, base_dep],
     )
 
-    helper = custom_target('frida-helper',
+    helper = custom_target('hs-helper',
       input: raw_helper,
       output: helper_name,
       command: post_process_helper,
diff --git a/src/socket/socket-host-session.vala b/src/socket/socket-host-session.vala
index 02605a2..1b08aa7 100644
--- a/src/socket/socket-host-session.vala
+++ b/src/socket/socket-host-session.vala
@@ -93,9 +93,9 @@ namespace Frida {
 				socket_connection = yield client.connect_async (connectable, cancellable);
 			} catch (GLib.Error e) {
 				if (e is IOError.CONNECTION_REFUSED)
-					throw new Error.SERVER_NOT_RUNNING ("Unable to connect to remote frida-server");
+					throw new Error.SERVER_NOT_RUNNING ("Unable to connect to remote agent");
 				else
-					throw new Error.SERVER_NOT_RUNNING ("Unable to connect to remote frida-server: %s", e.message);
+					throw new Error.SERVER_NOT_RUNNING ("Unable to connect to remote agent: %s", e.message);
 			}
 
 			Socket socket = socket_connection.socket;
@@ -145,7 +145,7 @@ namespace Frida {
 					auth_service = yield connection.get_proxy (null, ObjectPath.AUTHENTICATION_SERVICE,
 						DO_NOT_LOAD_PROPERTIES, cancellable);
 				} catch (IOError e) {
-					throw new Error.PROTOCOL ("Incompatible frida-server version");
+					throw new Error.PROTOCOL ("Incompatible agent version");
 				}
 
 				try {
@@ -160,7 +160,7 @@ namespace Frida {
 				host_session = yield connection.get_proxy (null, ObjectPath.HOST_SESSION, DO_NOT_LOAD_PROPERTIES,
 					cancellable);
 			} catch (IOError e) {
-				throw new Error.PROTOCOL ("Incompatible frida-server version");
+				throw new Error.PROTOCOL ("Incompatible agent version");
 			}
 
 			var entry = new HostEntry (connection, host_session, keepalive_interval);
